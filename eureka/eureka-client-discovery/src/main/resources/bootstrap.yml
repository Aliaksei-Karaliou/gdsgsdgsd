spring:
  application:
    name: cloud-config-client-ribbon

  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: cloud-config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/